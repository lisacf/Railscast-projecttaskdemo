<!DOCTYPE html>
<html>
<head>
  <title>Demoproject - <%= yield(:title) || "Projects and Tasks" %></title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <%= yield :head %>
</head>
<body>
	<h1>Demo Project</h1>
	<% flash.each do |key, msg| %>
	<%= content_tag :div, msg, :id => key %>
	<% end %>
	<div id="container">
		<h1><%= yield(:title) %></h1>
		<%= yield %>
	</div>
</body>
</html>
