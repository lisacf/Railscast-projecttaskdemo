<%= form_for @task do |f| %>
<%= f.label :name %>
<%= f.text_field :name %><br>
<%= f.label :due_at, "Date Due: " %>
<%= f.datetime_select :due_at %><br>
<%= f.label :project_id, "Project" %>
<%= f.collection_select :project_id, Project.all, :id, :name %><br>
<%= f.submit %>
<% end %>